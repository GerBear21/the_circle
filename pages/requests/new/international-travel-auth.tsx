// International Travel Authorization re-uses the travel-auth page component.
// The component detects the `international` pathname segment and switches to manual
// itinerary entry (no business-unit dropdown, no auto-km) and an ITA reference code.
export { default } from './travel-auth';
